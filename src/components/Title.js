const Title = () => {
  // first we add const then the name of the component then function
  return (
    // we write the return to send this function
    // after that we open big div and we put what ever we need in title and we can add more div
    <div>
      <h1>Cookies and Beyound</h1>
      <h2>where cookies maniacs gether</h2>
    </div>
  );
};
// now after we finish we should add export defult 'the function name '
export default Title;
